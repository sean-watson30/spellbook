<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head.ejs') %>
  <title>Wizard Spells</title>
</head>
<body>
  <div>
    <aside>
      <%- include('../partials/wizAside.ejs') %>
    </aside>
    <main>
      <nav>
        <h1 class="glowConst">Wizard Spells (6th Level)</h1>
        <a class="glow" href="/wizSpells/new">Transcribe a New Spell</a>
      </nav>
      <ul>
        <% for (let i = 0; i < spells.length; i++) { %>
        <div>
          <% if (spells[i].level == 6) { %>
            <a class="glow" href="/wizSpells/<%=spells[i]._id%>"><%=spells[i].name%></a>
          <% } else { %>
          <% } %>  
        </div>
        <% } %>
      </ul>
    </main>
  </div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head.ejs') %>
  <title><%= spell.name %></title>
</head>
<body>
  <!-- <header>
  </header>   -->
  <div>
    <aside>
      <%- include('../partials/wizAside.ejs') %>
    </aside>
    <main>
      <h1 class="glowConst"><%= spell.name %></h1>
      <h3 class="spellDesc">(<%= spell.school %>)</h3>
      <table>
        <tr>
          <td class="spellDesc"><b>Range:</b> <%= spell.range %></td>
          <td class="spellDesc"><b>Components:</b> <%= spell.components %></td>
        </tr>
        <tr>
          <td class="spellDesc"><b>Duration:</b> <%= spell.duration %></td>
          <td class="spellDesc"><b>Casting Time:</b> <%= spell.casting %></td>
        </tr>
        <tr>
          <td class="spellDesc"><b>Area of Effect:</b> <%= spell.aoe %></td>
          <td class="spellDesc"><b>Saving Throw:</b> <%= spell.saving %></td>  
        </tr>
      </table>
      <p class="spellDesc">
        <%= spell.description %>
      </p>
      <footer>
        <button onclick="location.href='/wizSpells/<%=spell._id%>/edit'" type="button">Edit: <%= spell.name %></button>
        <form action="/wizSpells/<%=spell._id%>?_method=DELETE" method="POST">
          <input type="submit" value="Remove <%= spell.name %> from Spellbook">
        </form>
      </footer>
    </main>
  </div>
</body>
</html>
<%  %>
<%=  %>
<%-  %>
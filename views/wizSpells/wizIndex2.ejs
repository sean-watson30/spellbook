<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head.ejs') %>
  <title>Wizard Spells</title>
</head>
<body>
  <div>
    <aside>
      <%- include('../partials/wizAside.ejs') %>
    </aside>
    <main>
      <nav>
        <h1>Wizard Spells (2nd Level)</h1>
        <a href="/wizSpells/new">Transcribe a New Spell</a>
      </nav>
      <ul>
        <% for (let i = 0; i < spells.length; i++) { %>
        <div>
          <% if (spells[i].level == 2) { %>
            <a class="glow" href="/wizSpells/<%=spells[i]._id%>"><%=spells[i].name%></a>
          <% } else { %>
          <% } %>  
        </div>
        <% } %>
      </ul>
    </main>
  </div>
</body>
</html>